# 🔴 КРИТИЧЕСКИЙ БАГ: Меню не отправляется после видео-инструкции

## Статус: ✅ ИСПРАВЛЕН

---

## 📋 КРАТКОЕ ОПИСАНИЕ

**Проблема:** Пользователи после оплаты получают видео-инструкцию "А теперь самое важное...", но **НЕ получают меню** с кнопками автоматически.

**Причина:** В функции `completeOnboarding()` отсутствует вызов `sendMenuMessage()`.

**Последствия:** Все пользователи, прошедшие онбординг, не видят меню и не знают как пользоваться ботом.

---

## ✅ ЧТО ИСПРАВЛЕНО

### Файл: `backend/src/modules/bot/post-payment-funnels.ts`

### Функция: `completeOnboarding()` (строки 256-290)

**Было:**
```typescript
// 4. Отправить видео-инструкцию по экосистеме клуба
await getTelegramService().sendVideo(chatId, '...', {...});

// 4. Запланировать воронку вовлечения
await scheduleEngagementFunnel(userId, chatId);
```

**Стало:**
```typescript
// 4. Отправить видео-инструкцию по экосистеме клуба
await getTelegramService().sendVideo(chatId, '...', {...});

// 5. Отправить меню с кнопками
await sendMenuMessage(chatId);

// 6. Запланировать воронку вовлечения
await scheduleEngagementFunnel(userId, chatId);
```

---

## 📊 КТО ПОСТРАДАЛ

### Критерии поиска пострадавших:
1. `is_pro = true` (оплатили подписку)
2. `onboarding_step = 'onboarding_complete'` (прошли онбординг)
3. `subscription_expires > NOW()` (подписка активна)
4. `created_at >= '2024-01-01'` (только недавние)

### SQL запрос для поиска:
```sql
-- См. файл: find_affected_users.sql
```

---

## 🔧 ПЛАН ИСПРАВЛЕНИЯ

### ✅ Шаг 1: Исправить код (ВЫПОЛНЕНО)
- [x] Добавлен вызов `sendMenuMessage(chatId)` в `completeOnboarding()`
- [x] Обновлена нумерация комментариев (4 → 5 → 6)

### ⚠️ Шаг 2: Деплой исправления
```bash
cd /Users/daniillepekhin/My\ Python/egiazarova/club_webapp/backend
git add .
git commit -m "🐛 fix: добавлена отправка меню после видео-инструкции в completeOnboarding()"
git push

# На сервере:
cd /path/to/backend
git pull
docker-compose restart ik-backend
```

### 📨 Шаг 3: Отправить меню пострадавшим пользователям

#### 3.1. Найти пострадавших:
```bash
cd /Users/daniillepekhin/My\ Python/egiazarova/club_webapp

# Выполнить SQL запрос
sshpass -p 'U3S%fZ(D2cru' ssh -o StrictHostKeyChecking=no root@31.128.36.81 \
  "PGPASSWORD='kH*kyrS&9z7K' psql -h 31.128.36.81 -p 5423 -U postgres -d club_hranitel" \
  < find_affected_users.sql
```

#### 3.2. Отправить меню всем:
```bash
chmod +x send_menu_to_affected.sh
./send_menu_to_affected.sh
```

**Что делает скрипт:**
1. Находит всех пострадавших пользователей
2. Показывает список
3. Просит подтверждение
4. Отправляет каждому POST запрос:
   ```bash
   curl -X POST https://hranitel.daniillepekhin.com/api/admin/manual-payment \
     -H "x-admin-secret: local-dev-secret" \
     -H "Content-Type: application/json" \
     -d '{"telegram_id": "...", "duration_days": 0}'
   ```
5. Выводит статистику

**Параметры:**
- `duration_days: 0` — не менять подписку, только отправить онбординг + меню

---

## 🧪 ТЕСТИРОВАНИЕ

### Проверить исправление:
1. Создать тестового пользователя
2. Пройти весь онбординг до конца
3. Нажать кнопку "готово"
4. **Проверить:** должно прийти ВИДЕО + МЕНЮ сразу после

### Ожидаемый результат:
```
1. Видео-инструкция "А теперь самое важное..."
2. Меню "ЭТО МЕНЮ УЧАСТНИКА КЛУБА КОД УСПЕХА ❤️"
   - инструкция
   - доступ в приложение
   - канал клуба
   - все материалы здесь
   - Пройти тест: какой я персонаж
   - подарить подписку
   - оферта
   - политика
```

---

## 📈 МОНИТОРИНГ

### После деплоя проверить логи:
```bash
# На сервере
docker logs ik-backend --tail 500 -f | grep "sendMenuMessage"
```

### Метрики для отслеживания:
- Количество пользователей с `onboarding_step = 'onboarding_complete'`
- Количество отправленных меню
- Количество ошибок при отправке меню

---

## 🔍 КОРНЕВАЯ ПРИЧИНА

### Почему баг появился?

1. **Функция `completeOnboarding()` была создана** для отправки видео-инструкции
2. **Планировалось**, что меню будет отправлено через callback `menu_back`
3. **НО:** пользователь не знает про кнопку `menu_back`, если ему не отправили меню!
4. **Результат:** замкнутый круг — меню не приходит, пользователь не знает как его получить

### Почему не заметили раньше?

1. Кнопка меню **есть** в левом нижнем углу (устанавливается в `setChatMenuButton`)
2. Команда `/menu` **работает** (устанавливается в `setMyCommands`)
3. **НО:** новые пользователи **не знают** про эти возможности без инструкции
4. Видео-инструкция **говорит** про кнопку меню, но **не показывает** как выглядит само меню

---

## 📝 ДОПОЛНИТЕЛЬНАЯ ИНФОРМАЦИЯ

### Связанные файлы:
- [x] `backend/src/modules/bot/post-payment-funnels.ts` — ИСПРАВЛЕНО
- [ ] `find_affected_users.sql` — SQL запрос для поиска пострадавших
- [ ] `send_menu_to_affected.sh` — скрипт для массовой отправки меню
- [ ] `MENU_PROBLEM_ANALYSIS.md` — детальный анализ проблемы

### Связанные функции:
- `completeOnboarding()` — отправляет видео-инструкцию (ИСПРАВЛЕНА)
- `sendMenuMessage()` — отправляет меню
- `startOnboardingAfterPayment()` — начало онбординга после оплаты
- `handleKeywordSuccess()` — обработка кодового слова "КАРТА"
- `scheduleEngagementFunnel()` — планирование воронки вовлечения

---

## ✅ CHECKLIST

### Разработка:
- [x] Баг исправлен в коде
- [x] Создан SQL запрос для поиска пострадавших
- [x] Создан скрипт для массовой отправки меню
- [x] Написана документация

### Деплой:
- [ ] Закоммитить изменения
- [ ] Задеплоить на сервер
- [ ] Перезапустить backend
- [ ] Проверить логи на ошибки

### Восстановление:
- [ ] Найти всех пострадавших пользователей
- [ ] Отправить им меню вручную
- [ ] Проверить что меню пришло

### Тестирование:
- [ ] Создать тестового пользователя
- [ ] Пройти весь онбординг
- [ ] Убедиться что меню приходит автоматически

---

## 🎯 ИТОГО

**Баг:** Меню не отправлялось после видео-инструкции
**Причина:** Отсутствие вызова `sendMenuMessage()` в `completeOnboarding()`
**Решение:** Добавлен вызов `await sendMenuMessage(chatId);`
**Статус:** ✅ ИСПРАВЛЕНО

**Следующие шаги:**
1. Деплой исправления
2. Отправка меню пострадавшим пользователям
3. Мониторинг работы исправления

---

**Дата:** 2026-02-06
**Автор:** Claude Code
**Приоритет:** 🔴 КРИТИЧЕСКИЙ
