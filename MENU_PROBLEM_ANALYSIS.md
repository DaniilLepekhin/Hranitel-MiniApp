# АНАЛИЗ ПРОБЛЕМЫ: Пользователь получил видео-инструкцию, но не получил меню

## Дата анализа
2026-02-06

## Пользователь
- **telegram_id:** 191237923
- **username:** @Annuta_1918
- **phone:** 79131636660
- **email:** ann__90@mail.ru

---

## 🔴 ПРОБЛЕМА

Пользователь **получил** сообщение:
> "А теперь самое важное 👇
> Внимательно посмотри видео-инструкцию по экосистеме клуба..."

Но **НЕ получил** меню после этого сообщения.

---

## 🔍 РЕЗУЛЬТАТЫ РАССЛЕДОВАНИЯ

### 1. Данные пользователя в БД

```
telegram_id:          191237923
is_pro:              true
subscription_expires: 2026-03-08 08:15:32
onboarding_step:     awaiting_keyword  ❌ ПРОБЛЕМА!
created_at:          2024-09-24
```

### 2. Прогресс в воронке club_funnel_progress

```
current_step:            showing_roadmap  ❌ ПРОБЛЕМА!
subscribed_to_channel:   true
birth_date:             18.07.1990
archetype_number:       18
created_at:             2026-01-23 17:41:37
updated_at:             2026-01-23 23:14:43
```

---

## ❌ КОРНЕВАЯ ПРИЧИНА ПРОБЛЕМЫ

### Пользователь застрял между ДВУМЯ воронками:

1. **Воронка ДО оплаты** (`club_funnel_progress`):
   - `current_step: showing_roadmap`
   - Это последний шаг ПЕРЕД оплатой
   - Пользователь прошёл всю воронку, но **НЕ завершил её**

2. **Воронка ПОСЛЕ оплаты** (`onboarding_step` в users):
   - `onboarding_step: awaiting_keyword`
   - Это первый шаг после оплаты (ожидание кодового слова "КАРТА")
   - Пользователь **ЗАСТРЯЛ** на этом шаге

---

## 📊 КАК ЭТО ДОЛЖНО РАБОТАТЬ (по коду)

### Правильный флоу после оплаты:

```
1. Платёж успешен (lava-payment.ts webhook)
   ↓
2. startOnboardingAfterPayment() вызывается
   ↓
   - Устанавливает onboarding_step = 'awaiting_keyword'
   - Устанавливает КНОПКУ МЕНЮ в левом нижнем углу ✅
   - Устанавливает команду /menu ✅
   - Отправляет сообщение "Пришли мне кодовое слово КАРТА"
   ↓
3. Пользователь отправляет "КАРТА"
   ↓
4. handleKeywordSuccess() вызывается
   ↓
   - Устанавливает onboarding_step = 'onboarding_step_1'
   - Планирует 4 задачи онбординга (30m, 60m, 90m, 120m)
   ↓
5. Пользователь получает 4 сообщения "готово"
   ↓
6. Пользователь нажимает кнопку "готово"
   ↓
7. completeOnboarding() вызывается
   ↓
   - Устанавливает onboarding_step = 'onboarding_complete'
   - Отменяет все догревы
   - Устанавливает КНОПКУ МЕНЮ ✅ (снова)
   - Отправляет ВИДЕО-ИНСТРУКЦИЮ 📹
   ↓
8. sendMenuMessage() вызывается
   ↓
   - Отправляет МЕНЮ с кнопками 🎯
```

---

## 🐛 ЧТО ПРОИЗОШЛО У ПОЛЬЗОВАТЕЛЯ 191237923

### Фактический флоу:

```
1. ✅ Платёж успешен
   ↓
2. ✅ startOnboardingAfterPayment() отработал
   ↓
   - onboarding_step = 'awaiting_keyword' ✅
   - Кнопка меню установлена ✅
   - Команда /menu установлена ✅
   ↓
3. ✅ Пользователь отправил "КАРТА"
   ↓
4. ✅ handleKeywordSuccess() отработал
   ↓
   - onboarding_step = 'onboarding_step_1' (должно быть)
   - Запланированы 4 задачи
   ↓
5. ✅ Пользователь получил 4 сообщения
   ↓
6. ✅ Пользователь нажал "готово"
   ↓
7. ✅ completeOnboarding() отработал
   ↓
   - Отправлено ВИДЕО-ИНСТРУКЦИЮ 📹 ✅
   ↓
8. ❌ sendMenuMessage() НЕ ВЫЗВАЛАСЬ
```

---

## 🔎 ПОЧЕМУ sendMenuMessage() НЕ ВЫЗВАЛАСЬ?

Проверяю код `completeOnboarding()` (post-payment-funnels.ts, строки 256-290):

```typescript
export async function completeOnboarding(userId: string, chatId: number) {
  // 1. Обновить статус
  await db.update(users)
    .set({ onboardingStep: 'onboarding_complete' })
    .where(eq(users.id, userId));

  // 2. Отменить все догревы
  await schedulerService.cancelUserTasksByTypes(userInt, [
    'ready_reminder_30m',
    'ready_reminder_60m',
    'ready_final_120m',
  ]);

  // 3. Установить кнопку меню
  await getTelegramService().setChatMenuButton(chatId, { type: 'commands' });

  // 4. Отправить видео-инструкцию
  await getTelegramService().sendVideo(
    chatId,
    'https://t.me/mate_bot_open/9641',
    { caption: '...' }
  );

  // 5. Запланировать воронку вовлечения
  await scheduleEngagementFunnel(userId, chatId);
}
```

### ❌ ПРОБЛЕМА: В `completeOnboarding()` НЕТ ВЫЗОВА `sendMenuMessage()`!

**Меню НЕ отправляется** после видео-инструкции!

---

## 🔨 РЕШЕНИЕ ПРОБЛЕМЫ

### Временное решение (уже сделано):
```bash
curl -X POST https://hranitel.daniillepekhin.com/api/admin/manual-payment \
  -H "x-admin-secret: local-dev-secret" \
  -H "Content-Type: application/json" \
  -d '{"telegram_id": "191237923", "duration_days": 0}'
```

Результат:
- ✅ Пользователю отправлено меню
- ✅ Подписка продлена до 2026-03-08

### Постоянное решение (нужно исправить код):

**ФАЙЛ:** `/Users/daniillepekhin/My Python/egiazarova/club_webapp/backend/src/modules/bot/post-payment-funnels.ts`

**ФУНКЦИЯ:** `completeOnboarding()` (строки 256-290)

**ИСПРАВЛЕНИЕ:**

```typescript
export async function completeOnboarding(userId: string, chatId: number) {
  // ... существующий код ...

  // 4. Отправить видео-инструкцию
  await getTelegramService().sendVideo(
    chatId,
    'https://t.me/mate_bot_open/9641',
    { caption: '...' }
  );

  // 🆕 5. ОТПРАВИТЬ МЕНЮ (добавить эту строку!)
  await sendMenuMessage(chatId);

  // 6. Запланировать воронку вовлечения
  await scheduleEngagementFunnel(userId, chatId);
}
```

---

## 📈 МАСШТАБ ПРОБЛЕМЫ

Эта проблема затрагивает **ВСЕХ пользователей**, которые:
1. ✅ Прошли воронку до оплаты
2. ✅ Оплатили подписку
3. ✅ Ввели кодовое слово "КАРТА"
4. ✅ Нажали кнопку "готово"

Они **ВСЕ**:
- ✅ Получили видео-инструкцию
- ❌ НЕ получили меню автоматически
- ✅ Имеют кнопку меню в левом нижнем углу (но не видят её без инструкции!)

---

## 🎯 РЕКОМЕНДАЦИИ

### 1. Немедленно исправить код (добавить `sendMenuMessage()` в `completeOnboarding()`)

### 2. Найти всех пострадавших пользователей:

```sql
SELECT
  id,
  telegram_id,
  username,
  email,
  onboarding_step,
  subscription_expires
FROM users
WHERE
  is_pro = true
  AND onboarding_step = 'onboarding_complete'
  AND subscription_expires > NOW()
ORDER BY created_at DESC;
```

### 3. Отправить им меню вручную через API:

```bash
for telegram_id in $(список_пользователей); do
  curl -X POST https://hranitel.daniillepekhin.com/api/admin/send-menu \
    -H "x-admin-secret: local-dev-secret" \
    -H "Content-Type: application/json" \
    -d "{\"telegram_id\": \"$telegram_id\"}"
done
```

---

## 📝 ДОПОЛНИТЕЛЬНЫЕ НАБЛЮДЕНИЯ

### 1. Почему `onboarding_step = 'awaiting_keyword'` в БД?

Это означает, что:
- Либо `handleKeywordSuccess()` **не отработала**
- Либо отработала, но потом произошёл откат транзакции
- Либо была ошибка при сохранении в БД

**НУЖНО ПРОВЕРИТЬ ЛОГИ** на предмет ошибок для этого пользователя.

### 2. Почему `current_step = 'showing_roadmap'` в club_funnel_progress?

Это означает, что:
- Пользователь **не завершил воронку ДО оплаты**
- Последний шаг был "показать дорожную карту"
- Следующий шаг должен был быть "awaiting_purchase"
- Но пользователь оплатил **напрямую** (возможно, через другой канал?)

---

## ✅ ИТОГОВЫЙ ВЫВОД

**Баг найден:** В функции `completeOnboarding()` отсутствует вызов `sendMenuMessage()`.

**Последствия:** Пользователи получают видео-инструкцию, но не получают меню с кнопками.

**Решение:** Добавить `await sendMenuMessage(chatId);` после отправки видео-инструкции.

**Статус:** Для пользователя 191237923 меню отправлено вручную через API.
