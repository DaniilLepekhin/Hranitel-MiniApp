# Анализ нового дизайна главного экрана

## 🎨 Что есть в новом дизайне Figma:

### 1. **Поиск** (новый элемент)
- Поле поиска с placeholder "Поиск..."
- Иконка лупы
- Темная плашка фона

### 2. **Приветствие** (изменено)
- **Текущее:** "Привет, {user.firstName}! 👋" + "Как ты себя чувствуешь сегодня?"
- **Новое:** "Привет, {Имя}!" (крупный шрифт TT Nooks 54px) + "Ты в пространстве клуба «Код Денег»" (TT Nooks 21px)

### 3. **Мой баланс** (изменено)
- **Текущее:** Карточка "Энергии" с числом и иконкой молнии
- **Новое:** Отдельный блок "Мой баланс" с большим числом (1250) + текст "энергий"
- Более крупный и заметный дизайн

### 4. **Пригласи друга** (новый блок)
- Текст: "Пригласи друга в клуб КОД ДЕНЕГ"
- Логотип КОД
- Ссылка с текстом "Отправьте эту ссылку другу"
- Иконка копирования

### 5. **Анонсы** (новый раздел)
- Заголовок "Анонсы"
- Иконка рупора (bullhorn)

### 6. **Нижнее меню** (новое)
- 5 пунктов меню:
  1. Главная
  2. Путь
  3. Чаты
  4. Рейтинги / баллы
  5. Профиль

## ❌ Что убрано из текущего дизайна:

1. ~~Stats Cards (Streak/Level/Energy)~~ → заменено на "Мой баланс"
2. ~~КОД ДЕНЕГ 4.0 Widgets (Next Stream / Deadline)~~ → убрано
3. ~~Level Progress~~ → убрано
4. ~~Quick Actions (Медитация дня / Продолжить)~~ → убрано
5. ~~Recommended Courses~~ → убрано

## 📋 План реализации:

### Что нужно добавить в [HomeTab.tsx](webapp/src/components/tabs/HomeTab.tsx):

1. **Поисковая строка** (вверху)
   - Input с placeholder "Поиск..."
   - Иконка лупы
   - Тёмный фон (#2d2620)

2. **Обновить приветствие**
   - Заменить текст и стиль
   - Использовать более крупный шрифт для имени

3. **Переделать блок баланса**
   - Убрать мелкие карточки
   - Сделать один большой блок "Мой баланс"
   - Крупное число энергий

4. **Добавить блок "Пригласи друга"**
   - Реферальная ссылка
   - Кнопка копирования
   - Логотип КОД

5. **Добавить раздел "Анонсы"**
   - Заголовок
   - Список анонсов (данные с бэкенда)

6. **Нижнее меню навигации**
   - Заменить текущие табы
   - 5 пунктов с иконками

## 🔧 Технические детали:

### Цвета:
- Фон поиска: `#2d2620`
- Текст placeholder: `#f7f1e8` (opacity 0.7)
- Основной текст: `#3d2f1f`
- Вторичный текст: `#6b5a4a`

### Шрифты:
- TT Nooks 54px (weight 300) - приветствие имя
- TT Nooks 21px (weight 300) - подзаголовок
- TT Nooks 24px (weight 300) - "Мой баланс"
- Gilroy 46px (weight 600) - число энергий
- Gilroy 13px (weight 600) - остальные элементы

### API endpoints (нужно добавить):
```typescript
// Поиск
const searchApi = {
  search: async (query: string) => { ... }
};

// Реферальная система
const referralApi = {
  getLink: async (userId: string) => { ... },
  getStats: async (userId: string) => { ... }
};

// Анонсы
const announcementsApi = {
  getList: async () => { ... }
};
```

## ⚠️ Вопросы к дизайну:

1. Нужно ли сохранить функциональность streak/level/progress?
2. Куда перенести виджеты "Next Stream" и "Deadline"?
3. Куда перенести "Медитация дня" и курсы?
4. Как работает поиск - что ищем?
5. Как генерируется реферальная ссылка?
