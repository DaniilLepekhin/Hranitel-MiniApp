# 🔍 Отладка Проблемы с Балансом Энергий

## Проблема
После начисления `#созвон +100⚡`, баланс показывает **0** вместо **100**.

## Что Мы Уже Проверили

### ✅ База Данных - ПРАВИЛЬНО
```bash
cd backend
bun run check-energy-transactions.ts anatolyi_tester
# Результат: 1 транзакция, +100⚡
# Поле users.energies: 100⚡
```

### ✅ Backend API - ПРАВИЛЬНО
- Endpoint: `GET /api/v1/energies/balance`
- Метод `getBalance()` возвращает `users.energies`
- Транзакция сохранена корректно

### ❌ Frontend - ПРОБЛЕМА ЗДЕСЬ
- React Query кэширует данные
- `placeholderData: { balance: 0 }` показывает 0 при загрузке
- Telegram WebApp агрессивно кэширует

---

## 🚀 Новый Деплой (db59c5e)

### Что Исправлено:
1. **Убрана кэширование полностью:**
   - `staleTime: 0` (всегда свежие данные)
   - `gcTime: 0` (не хранить в памяти)
   - `refetchOnMount: 'always'` (перезагрузка при каждом открытии)

2. **Убран placeholderData:**
   - Вместо `0` показывается `...` при загрузке
   - Теперь видно, когда данные реально загружаются

3. **Добавлено логирование:**
   - Консоль покажет все запросы к API
   - Видно, что именно возвращает backend
   - Можно диагностировать, где теряются данные

---

## 📱 Инструкция для Тестирования

### Шаг 1: Подождать Деплоя (3-5 минут)
```bash
gh run watch 21896574089
# Или проверить: https://github.com/DaniilLepekhin/Hranitel-MiniApp/actions
```

### Шаг 2: Открыть DevTools в Telegram Desktop
1. **Telegram Desktop** (не мобильное приложение!)
2. **Windows/Linux:** `Ctrl + Shift + I`
3. **macOS:** `Cmd + Option + I`
4. Перейти во вкладку **Console**

### Шаг 3: Открыть Mini App
1. Найти бота `@hranitel_miniapp_bot` (или свой)
2. Нажать **Menu** → Открыть приложение
3. ИЛИ отправить команду `/start`

### Шаг 4: Перейти в Рейтинги
1. Нажать на вкладку **"Рейтинги"** (иконка с кубком)
2. Наблюдать **Console** в DevTools

### Шаг 5: Проверить Логи
В консоли должно появиться:
```
[RatingsTab] Fetching energy balance for user: 53b607bb-ed41-4b1c-9789-d34f20792edc
[RatingsTab] Energy balance response: { success: true, balance: 100 }
[RatingsTab] Balance data updated: { success: true, balance: 100 }
```

### Шаг 6: Что Проверять

#### ✅ Если видите `balance: 100`:
- **API работает правильно!**
- Проблема была в кэшировании
- Баланс должен отображаться на экране

#### ❌ Если видите `balance: 0`:
- **API возвращает неправильные данные**
- Проблема в backend
- Нужно проверить:
  1. JWT токен валидный?
  2. User ID правильный?
  3. Backend читает из правильной БД?

#### ❌ Если видите ошибку 401 Unauthorized:
- **Проблема с авторизацией**
- JWT токен недействителен
- Нужно:
  1. Перезайти в Mini App
  2. Проверить `/api/v1/auth/telegram` endpoint

#### ❌ Если ничего не видите:
- **API не отвечает**
- Backend не работает
- Проверить:
  1. Backend запущен?
  2. `https://app.successkod.com/api/v1/energies/balance` доступен?

---

## 🔧 Дополнительная Отладка

### Проверить API Вручную (через cURL)

#### 1. Получить JWT Token:
```bash
cd backend
bun run test-api-balance.ts anatolyi_tester
# Скопировать токен из вывода
```

#### 2. Проверить API:
```bash
curl -H "Authorization: Bearer YOUR_JWT_TOKEN" \
     https://app.successkod.com/api/v1/energies/balance
```

**Ожидаемый ответ:**
```json
{
  "success": true,
  "balance": 100
}
```

### Проверить Историю Транзакций:
```bash
curl -H "Authorization: Bearer YOUR_JWT_TOKEN" \
     https://app.successkod.com/api/v1/energies/history?limit=10
```

**Ожидаемый ответ:**
```json
{
  "success": true,
  "transactions": [
    {
      "id": "...",
      "userId": "53b607bb-ed41-4b1c-9789-d34f20792edc",
      "amount": 100,
      "type": "income",
      "reason": "Участие в Созвоне",
      "metadata": {"hashtag": "#созвон", "chat_type": "city"},
      "createdAt": "2026-02-11T11:15:41.000Z"
    }
  ]
}
```

---

## 🐛 Известные Проблемы Telegram WebApp

### 1. Агрессивное Кэширование
Telegram может кэшировать JavaScript файлы на **несколько часов**.

**Решение:**
- Добавить cache busting в URL
- Использовать Telegram Desktop для тестирования
- Очистить кэш: `Settings → Advanced → Clear cache`

### 2. Service Worker
Если есть Service Worker, он может отдавать старые данные.

**Проверка:**
```javascript
// В DevTools Console:
navigator.serviceWorker.getRegistrations().then(registrations => {
  registrations.forEach(registration => {
    console.log('Service Worker:', registration);
    registration.unregister();
  });
});
```

### 3. IndexedDB/LocalStorage
React Query может сохранять данные в браузере.

**Очистка:**
```javascript
// В DevTools Console:
localStorage.clear();
indexedDB.databases().then(dbs => {
  dbs.forEach(db => indexedDB.deleteDatabase(db.name));
});
```

---

## 📊 Следующие Шаги

### Если баланс всё равно 0:

1. **Проверить DevTools Console** - что показывает API?
2. **Скриншот логов** - отправить в чат
3. **Проверить Network Tab** - есть ли запрос к `/api/v1/energies/balance`?
4. **Response** - что возвращает backend?

### Если API возвращает 0:

```bash
# Проверить БД напрямую:
cd backend
bun run sync-energy-balance.ts anatolyi_tester

# Проверить, какой userId приходит в API:
# Добавить логирование в backend/src/modules/energy-points/index.ts
```

### Если API не вызывается:

```bash
# Проверить токен:
cd webapp
# В src/lib/api.ts добавить:
console.log('Auth token:', authToken);

# Проверить, что токен передаётся:
console.log('Headers:', headers);
```

---

## ✅ Критерии Успеха

### После деплоя должно работать:
1. ✅ Открыть Mini App
2. ✅ Перейти в "Рейтинги"
3. ✅ Баланс показывает **100⚡** (не 0!)
4. ✅ Нажать "История начислений"
5. ✅ Видна транзакция "#созвон +100⚡"
6. ✅ Console показывает `balance: 100`

---

## 📞 Контакты для Отладки

Если проблема не решается:
1. Скриншот **Console** (DevTools)
2. Скриншот **Network Tab** (запрос `/energies/balance`)
3. Скриншот **экрана** с балансом
4. Username: `@anatolyi_tester`

---

**Статус:** Деплой запущен (21896574089)  
**ETA:** ~3-5 минут  
**Ветка:** `main`  
**Коммит:** `db59c5e`  
**Дата:** 11 февраля 2026, 10:37 MSK
